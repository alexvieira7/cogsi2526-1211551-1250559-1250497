name: CA6 Part1 Rollback (GitHub Actions)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Stable tag (ex: stable-v5)'
        required: true

permissions:
  contents: write

jobs:
  rollback:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build jar for selected tag (estratégia simples)
        working-directory: CA6/spring-example
        run: |
          git checkout ${{ github.event.inputs.tag }}
          ./gradlew clean build

      - name: Preparar artefacto de rollback
        working-directory: CA6
        run: |
          mkdir -p alternativa/artifacts
          cp spring-example/build/libs/*.jar alternativa/artifacts/app-${{ github.event.inputs.tag }}.jar

      - name: Run rollback playbook
        working-directory: CA6/alternativa
        run: |
          ansible-playbook -i ansible/inventory.ini ansible/rollback.yml \
            --extra-vars "tag=${{ github.event.inputs.tag }}"
